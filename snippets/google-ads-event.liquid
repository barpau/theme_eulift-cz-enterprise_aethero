
  {% if template == 'cart' %}
<script>
  gtag('event', 'page_view', {
    'send_to': 'AW-993928973',
    'value': {{ cart.total_price | money_without_currency | remove: ',' }},
    'items': [
      {% for item in cart.items %}
      {
        'id': '{{ item.product_id }}',
        'google_business_vertical': 'retail'
      }{% unless forloop.last %},{% endunless %}
      {% endfor %}
    ]
  });
</script>
{% endif %}
